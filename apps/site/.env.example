# Build-time env for the qrnlab-site build (Workers Builds / CI).
# The content loader falls back to the local D1 file in dev; these are used
# when no local D1 is available.
BUILD_API_URL="https://app.qrnlab.org"
BUILD_TOKEN=""
